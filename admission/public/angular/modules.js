(function() {
    'use strict';

    angular.module('ng.admission', [
            'ui.router',
        ])
        .run(['$rootScope',
            function moduleRun($rootScope) {

            }
        ]);
}());
